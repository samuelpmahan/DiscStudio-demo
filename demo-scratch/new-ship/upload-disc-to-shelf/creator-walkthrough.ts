export type CreatorWalkthroughItem = Readonly<{
  id: string; label: string; reviewId: string; action: Readonly<{ href: `./creator-review.html#${string}` }>;
  creator: Readonly<{ do: string; expect: string; recovery: string }>;
  developer: Readonly<{ input: string; calculation: string; output: string; owningSource: string }>;
  evidence: Readonly<{ whatToObserve: string; exactCheckpoint: string }>;
}>;
export type CreatorWalkthroughGroup = Readonly<{ id: string; label: string; items: readonly CreatorWalkthroughItem[] }>;
const item = (id: string, label: string, doText: string, expect: string, recovery: string, input: string, calculation: string, output: string, owningSource: string, evidence: string, checkpoint: string): CreatorWalkthroughItem => ({
  id, label, reviewId: `creator-review-${id}`, action: { href: `./creator-review.html#creator-review-${id}` },
  creator: { do: doText, expect, recovery }, developer: { input, calculation, output, owningSource }, evidence: { whatToObserve: evidence, exactCheckpoint: checkpoint },
});

/** Same source feeds the creator guide and the pinned Neat inspection rows. */
export const creatorWalkthroughGroups: readonly CreatorWalkthroughGroup[] = [
  { id: 'start', label: '1. Start fresh', items: [item('start-fresh', 'Start the demo fresh', 'Open the demo and choose Add a photo as your first action.', 'The empty current-session bag is ready; prior session material is retained but not shown.', 'Reload for another fresh session; do not expect old bags to reappear in this MVP.', 'Page load and browser storage access.', 'persistent-experience.ts creates a unique current-session key.', 'Fresh photo-first creator state.', 'app.ts; persistent-experience.ts; upload-ui.ts · mountUpload', 'First visible decision is Add a photo, with no restored shelf or bag.', 'Fresh page before a photo is chosen.')] },
  { id: 'photo', label: '2. Add a photo', items: [item('photo-load-error-cancel', 'Load, cancel, or reject a photo', 'Choose a PNG, JPEG, or WebP photo. Also try cancel and an unreadable file.', 'A usable photo opens correction; cancel leaves the form usable; an error never becomes a saved photo.', 'Choose another file; a previously loaded source remains until you replace it.', 'Local file and source pixels.', 'upload-ui.ts decodes locally and retains the original while correction is active.', 'Prepared photo draft or a readable non-save error.', 'upload-ui.ts · photo input and crop flow; model.ts · addDraftPhoto', 'Photo loads locally; original remains available for a different crop.', 'After load, cancel, and invalid-file attempts.')] },
  { id: 'prepare', label: '3. Prepare the image', items: [item('prepare-circles', 'Choose and correct the circle', 'Choose the closest complete rim from Circle 1, Circle 2, or Circle 3. Optionally use Refine choice while keeping the original choice available, Find other circles if needed, or Adjust manually; then choose Use photo.', 'Use photo applies the visible circle to the current photo draft.', 'Try another circle, Find other circles, or adjust manually before Use photo.', 'Original pixels and candidate circles.', 'circle-fit.ts proposes circles; upload-ui.ts keeps selection/refinement/manual control explicit.', 'One retained cropped photo for description.', 'circle-fit.ts; upload-ui.ts · circle candidate and crop controls', 'The applied photo matches the visible circle and no ellipse fitting is claimed.', 'Immediately after Use photo.')] },
  { id: 'describe', label: '4. Describe the disc', items: [item('describe-facts', 'Name the disc; leave optional facts unknown', 'Choose manufacturer and mold. Add nickname, flight overrides, plastic, and weight only when known.', 'Mold is required. Plastic, flight overrides, weight, and nickname are optional; blank flight overrides inherit known mold facts, while an explicit blank override marks that field unknown. Blank plastic means unknown and Save remains available.', 'Choose a different mold or clear optional facts; do not guess.', 'Prepared photo and selected mold fields.', 'upload-ui.ts validates the chosen mold; model.ts · checkDraft validates only supported facts.', 'A saveable photo disc draft with declared unknowns.', 'upload-ui.ts · suggestPlastics/updateSaveState; model.ts · checkDraft/save', 'Save is enabled with blank or explicit Unknown plastic once photo and mold are ready.', 'Immediately before Save.')] },
  { id: 'bag', label: '5. Build this session bag', items: [item('bag-save-reuse', 'Save and reuse this session bag', 'Save one or more prepared discs, then select them from Today’s Bag. Add the next disc and confirm the cleared photo, nickname, and flight overrides do not reuse the prior draft.', 'The current bag contains N saved discs and each retains its photo and entered facts; the next draft starts clean for another current-session save.', 'If saving fails, correct the shown storage error and retry; no completed-save claim is shown.', 'Validated photo disc draft.', 'model.ts · createExperience.save composes disc, depiction, shelf, and bag Parts before persistence.', 'Current-session bag row with PxC provenance; durable archive hidden for later release.', 'model.ts · createExperience.save; persistent-experience.ts; persistence.ts', 'Saved photo and metadata appear in Today’s Bag; legacy/session archives are not surfaced.', 'After each Save returns.')] },
  { id: 'queue', label: '6. Select, preview, and queue', items: [item('queue-explicit', 'Select 1..N, preview, then enqueue', 'Select one or more bag cards, choose layout and orientation, preview, then explicitly choose Add to output queue. Repeat with another selection or style.', 'Each queued card holds its own exact disc/photo/layout/orientation; later changes do not rewrite it.', 'Change the current selection or style and preview again; remove a queued card if it should not export.', 'Selected bag rows and chosen card style.', 'model.ts · enqueueOutput composes an immutable PxC output queue; export-ui.ts previews only the current snapshot.', 'Inspectable, removable queued output entries.', 'export-ui.ts; model.ts · enqueueOutput/removeOutput; export-queue-core.ts · queueCards', 'No card is queued automatically; two passes can leave mixed styles and duplicate disc outputs.', 'After Add to output queue, and after a second differently styled enqueue.')] },
  { id: 'export', label: '7. Export the queue', items: [item('export-queue-zip', 'Export only the existing queue', 'With at least one queued card, choose Export queue ZIP and inspect its files in your editor.', 'One ZIP contains each queued PNG plus manifest.json; repeated disc/style additions have unique filenames. Cards use 1080×1920 vertical or 1920×1080 horizontal frames with alpha outside the card.', 'If packaging fails, fix the shown error and retry; the queue remains intact. Download completion itself is not assumed.', 'Immutable PxC queue entries.', 'browser-export.ts uses export-queue-core.ts prepareExport and shared browser-card-renderer.ts.', 'ZIP download request; queue remains for retry or inspection.', 'export-ui.ts; browser-export.ts; export-queue-core.ts; browser-card-renderer.ts; zip.ts', 'ZIP contents, dimensions, alpha, and editor import match the held queue.', 'Opened ZIP and one PNG checked in the intended editor.')] },
  { id: 'device', label: '8. Check a real device', items: [item('device-real', 'Use an actual iPhone or target device', 'On a real device, test touch crop, keyboard, app switching, photo picker, and ZIP download.', 'Record each observed behavior for that exact device/browser.', 'Mark an unrun or inconclusive behavior unknown; retry on the actual device.', 'Real device and browser version.', 'Human-operated browser behavior; no repository calculation certifies it.', 'Device-specific evidence or an explicit unknown.', 'Real-device review; no automated device claim in this source.', 'Actual touch and download evidence, not simulator inference.', 'Named device/browser session after a complete creator run.')] },
  { id: 'review', label: '9. Review and accept', items: [item('review-checkpoint', 'Review normal and adverse paths', 'Review the normal path and try cancel, invalid photo, failed crop, unknown facts, storage failure, empty queue, preview race, and ZIP failure.', 'Every failure gives a next action without claiming success; known limits remain visible.', 'Capture the failure, recover with the stated action, and have a human record acceptance separately.', 'Normal and adversarial actions with resulting visible state.', 'Repository tests cover bounded data/ZIP behavior; creator inspection remains human.', 'Human inspection record distinct from acceptance.', 'creator-walkthrough.ts; experiences.test.ts; export-queue.test.ts', 'Limits include unverified real-device download completion and no full formal Neat PQL/PCR ledger.', 'Named human review of this build checkpoint and its evidence.')] },
] as const;
export const creatorWalkthrough = Object.freeze({ groups: creatorWalkthroughGroups });
